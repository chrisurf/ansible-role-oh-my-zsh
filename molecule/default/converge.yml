---
- name: Converge
  hosts: all

  vars:
    omz_user: root

  tasks:

    # git and curl are installed by the role itself, so no extra
    # pre-tasks are required here.
    - name: "Include chrisurf.oh_my_zsh"
      ansible.builtin.include_role:
        name: "chrisurf.oh_my_zsh"
...
